// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/altierawr/vstreamer/ent/audiocodec"
	"github.com/altierawr/vstreamer/ent/audiotrack"
	"github.com/altierawr/vstreamer/ent/playsessionmedia"
	"github.com/altierawr/vstreamer/ent/predicate"
)

// AudioTrackQuery is the builder for querying AudioTrack entities.
type AudioTrackQuery struct {
	config
	ctx             *QueryContext
	order           []audiotrack.OrderOption
	inters          []Interceptor
	predicates      []predicate.AudioTrack
	withCodecs      *AudioCodecQuery
	withMedia       *PlaySessionMediaQuery
	withFKs         bool
	modifiers       []func(*sql.Selector)
	loadTotal       []func(context.Context, []*AudioTrack) error
	withNamedCodecs map[string]*AudioCodecQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AudioTrackQuery builder.
func (atq *AudioTrackQuery) Where(ps ...predicate.AudioTrack) *AudioTrackQuery {
	atq.predicates = append(atq.predicates, ps...)
	return atq
}

// Limit the number of records to be returned by this query.
func (atq *AudioTrackQuery) Limit(limit int) *AudioTrackQuery {
	atq.ctx.Limit = &limit
	return atq
}

// Offset to start from.
func (atq *AudioTrackQuery) Offset(offset int) *AudioTrackQuery {
	atq.ctx.Offset = &offset
	return atq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (atq *AudioTrackQuery) Unique(unique bool) *AudioTrackQuery {
	atq.ctx.Unique = &unique
	return atq
}

// Order specifies how the records should be ordered.
func (atq *AudioTrackQuery) Order(o ...audiotrack.OrderOption) *AudioTrackQuery {
	atq.order = append(atq.order, o...)
	return atq
}

// QueryCodecs chains the current query on the "codecs" edge.
func (atq *AudioTrackQuery) QueryCodecs() *AudioCodecQuery {
	query := (&AudioCodecClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(audiotrack.Table, audiotrack.FieldID, selector),
			sqlgraph.To(audiocodec.Table, audiocodec.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, audiotrack.CodecsTable, audiotrack.CodecsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMedia chains the current query on the "media" edge.
func (atq *AudioTrackQuery) QueryMedia() *PlaySessionMediaQuery {
	query := (&PlaySessionMediaClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(audiotrack.Table, audiotrack.FieldID, selector),
			sqlgraph.To(playsessionmedia.Table, playsessionmedia.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, audiotrack.MediaTable, audiotrack.MediaColumn),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AudioTrack entity from the query.
// Returns a *NotFoundError when no AudioTrack was found.
func (atq *AudioTrackQuery) First(ctx context.Context) (*AudioTrack, error) {
	nodes, err := atq.Limit(1).All(setContextOp(ctx, atq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{audiotrack.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (atq *AudioTrackQuery) FirstX(ctx context.Context) *AudioTrack {
	node, err := atq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AudioTrack ID from the query.
// Returns a *NotFoundError when no AudioTrack ID was found.
func (atq *AudioTrackQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = atq.Limit(1).IDs(setContextOp(ctx, atq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{audiotrack.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (atq *AudioTrackQuery) FirstIDX(ctx context.Context) int {
	id, err := atq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AudioTrack entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AudioTrack entity is found.
// Returns a *NotFoundError when no AudioTrack entities are found.
func (atq *AudioTrackQuery) Only(ctx context.Context) (*AudioTrack, error) {
	nodes, err := atq.Limit(2).All(setContextOp(ctx, atq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{audiotrack.Label}
	default:
		return nil, &NotSingularError{audiotrack.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (atq *AudioTrackQuery) OnlyX(ctx context.Context) *AudioTrack {
	node, err := atq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AudioTrack ID in the query.
// Returns a *NotSingularError when more than one AudioTrack ID is found.
// Returns a *NotFoundError when no entities are found.
func (atq *AudioTrackQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = atq.Limit(2).IDs(setContextOp(ctx, atq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{audiotrack.Label}
	default:
		err = &NotSingularError{audiotrack.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (atq *AudioTrackQuery) OnlyIDX(ctx context.Context) int {
	id, err := atq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AudioTracks.
func (atq *AudioTrackQuery) All(ctx context.Context) ([]*AudioTrack, error) {
	ctx = setContextOp(ctx, atq.ctx, "All")
	if err := atq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AudioTrack, *AudioTrackQuery]()
	return withInterceptors[[]*AudioTrack](ctx, atq, qr, atq.inters)
}

// AllX is like All, but panics if an error occurs.
func (atq *AudioTrackQuery) AllX(ctx context.Context) []*AudioTrack {
	nodes, err := atq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AudioTrack IDs.
func (atq *AudioTrackQuery) IDs(ctx context.Context) (ids []int, err error) {
	if atq.ctx.Unique == nil && atq.path != nil {
		atq.Unique(true)
	}
	ctx = setContextOp(ctx, atq.ctx, "IDs")
	if err = atq.Select(audiotrack.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (atq *AudioTrackQuery) IDsX(ctx context.Context) []int {
	ids, err := atq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (atq *AudioTrackQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, atq.ctx, "Count")
	if err := atq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, atq, querierCount[*AudioTrackQuery](), atq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (atq *AudioTrackQuery) CountX(ctx context.Context) int {
	count, err := atq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (atq *AudioTrackQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, atq.ctx, "Exist")
	switch _, err := atq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (atq *AudioTrackQuery) ExistX(ctx context.Context) bool {
	exist, err := atq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AudioTrackQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (atq *AudioTrackQuery) Clone() *AudioTrackQuery {
	if atq == nil {
		return nil
	}
	return &AudioTrackQuery{
		config:     atq.config,
		ctx:        atq.ctx.Clone(),
		order:      append([]audiotrack.OrderOption{}, atq.order...),
		inters:     append([]Interceptor{}, atq.inters...),
		predicates: append([]predicate.AudioTrack{}, atq.predicates...),
		withCodecs: atq.withCodecs.Clone(),
		withMedia:  atq.withMedia.Clone(),
		// clone intermediate query.
		sql:  atq.sql.Clone(),
		path: atq.path,
	}
}

// WithCodecs tells the query-builder to eager-load the nodes that are connected to
// the "codecs" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AudioTrackQuery) WithCodecs(opts ...func(*AudioCodecQuery)) *AudioTrackQuery {
	query := (&AudioCodecClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withCodecs = query
	return atq
}

// WithMedia tells the query-builder to eager-load the nodes that are connected to
// the "media" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AudioTrackQuery) WithMedia(opts ...func(*PlaySessionMediaQuery)) *AudioTrackQuery {
	query := (&PlaySessionMediaClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withMedia = query
	return atq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AudioTrack.Query().
//		GroupBy(audiotrack.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (atq *AudioTrackQuery) GroupBy(field string, fields ...string) *AudioTrackGroupBy {
	atq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AudioTrackGroupBy{build: atq}
	grbuild.flds = &atq.ctx.Fields
	grbuild.label = audiotrack.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.AudioTrack.Query().
//		Select(audiotrack.FieldName).
//		Scan(ctx, &v)
func (atq *AudioTrackQuery) Select(fields ...string) *AudioTrackSelect {
	atq.ctx.Fields = append(atq.ctx.Fields, fields...)
	sbuild := &AudioTrackSelect{AudioTrackQuery: atq}
	sbuild.label = audiotrack.Label
	sbuild.flds, sbuild.scan = &atq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AudioTrackSelect configured with the given aggregations.
func (atq *AudioTrackQuery) Aggregate(fns ...AggregateFunc) *AudioTrackSelect {
	return atq.Select().Aggregate(fns...)
}

func (atq *AudioTrackQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range atq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, atq); err != nil {
				return err
			}
		}
	}
	for _, f := range atq.ctx.Fields {
		if !audiotrack.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if atq.path != nil {
		prev, err := atq.path(ctx)
		if err != nil {
			return err
		}
		atq.sql = prev
	}
	return nil
}

func (atq *AudioTrackQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AudioTrack, error) {
	var (
		nodes       = []*AudioTrack{}
		withFKs     = atq.withFKs
		_spec       = atq.querySpec()
		loadedTypes = [2]bool{
			atq.withCodecs != nil,
			atq.withMedia != nil,
		}
	)
	if atq.withMedia != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, audiotrack.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AudioTrack).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AudioTrack{config: atq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(atq.modifiers) > 0 {
		_spec.Modifiers = atq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, atq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := atq.withCodecs; query != nil {
		if err := atq.loadCodecs(ctx, query, nodes,
			func(n *AudioTrack) { n.Edges.Codecs = []*AudioCodec{} },
			func(n *AudioTrack, e *AudioCodec) { n.Edges.Codecs = append(n.Edges.Codecs, e) }); err != nil {
			return nil, err
		}
	}
	if query := atq.withMedia; query != nil {
		if err := atq.loadMedia(ctx, query, nodes, nil,
			func(n *AudioTrack, e *PlaySessionMedia) { n.Edges.Media = e }); err != nil {
			return nil, err
		}
	}
	for name, query := range atq.withNamedCodecs {
		if err := atq.loadCodecs(ctx, query, nodes,
			func(n *AudioTrack) { n.appendNamedCodecs(name) },
			func(n *AudioTrack, e *AudioCodec) { n.appendNamedCodecs(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range atq.loadTotal {
		if err := atq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (atq *AudioTrackQuery) loadCodecs(ctx context.Context, query *AudioCodecQuery, nodes []*AudioTrack, init func(*AudioTrack), assign func(*AudioTrack, *AudioCodec)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*AudioTrack)
	nids := make(map[int]map[*AudioTrack]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(audiotrack.CodecsTable)
		s.Join(joinT).On(s.C(audiocodec.FieldID), joinT.C(audiotrack.CodecsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(audiotrack.CodecsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(audiotrack.CodecsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*AudioTrack]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*AudioCodec](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "codecs" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (atq *AudioTrackQuery) loadMedia(ctx context.Context, query *PlaySessionMediaQuery, nodes []*AudioTrack, init func(*AudioTrack), assign func(*AudioTrack, *PlaySessionMedia)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AudioTrack)
	for i := range nodes {
		if nodes[i].play_session_media_audio_tracks == nil {
			continue
		}
		fk := *nodes[i].play_session_media_audio_tracks
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(playsessionmedia.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "play_session_media_audio_tracks" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (atq *AudioTrackQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := atq.querySpec()
	if len(atq.modifiers) > 0 {
		_spec.Modifiers = atq.modifiers
	}
	_spec.Node.Columns = atq.ctx.Fields
	if len(atq.ctx.Fields) > 0 {
		_spec.Unique = atq.ctx.Unique != nil && *atq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, atq.driver, _spec)
}

func (atq *AudioTrackQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(audiotrack.Table, audiotrack.Columns, sqlgraph.NewFieldSpec(audiotrack.FieldID, field.TypeInt))
	_spec.From = atq.sql
	if unique := atq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if atq.path != nil {
		_spec.Unique = true
	}
	if fields := atq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, audiotrack.FieldID)
		for i := range fields {
			if fields[i] != audiotrack.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := atq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := atq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := atq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := atq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (atq *AudioTrackQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(atq.driver.Dialect())
	t1 := builder.Table(audiotrack.Table)
	columns := atq.ctx.Fields
	if len(columns) == 0 {
		columns = audiotrack.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if atq.sql != nil {
		selector = atq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if atq.ctx.Unique != nil && *atq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range atq.predicates {
		p(selector)
	}
	for _, p := range atq.order {
		p(selector)
	}
	if offset := atq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := atq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedCodecs tells the query-builder to eager-load the nodes that are connected to the "codecs"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (atq *AudioTrackQuery) WithNamedCodecs(name string, opts ...func(*AudioCodecQuery)) *AudioTrackQuery {
	query := (&AudioCodecClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if atq.withNamedCodecs == nil {
		atq.withNamedCodecs = make(map[string]*AudioCodecQuery)
	}
	atq.withNamedCodecs[name] = query
	return atq
}

// AudioTrackGroupBy is the group-by builder for AudioTrack entities.
type AudioTrackGroupBy struct {
	selector
	build *AudioTrackQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (atgb *AudioTrackGroupBy) Aggregate(fns ...AggregateFunc) *AudioTrackGroupBy {
	atgb.fns = append(atgb.fns, fns...)
	return atgb
}

// Scan applies the selector query and scans the result into the given value.
func (atgb *AudioTrackGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, atgb.build.ctx, "GroupBy")
	if err := atgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AudioTrackQuery, *AudioTrackGroupBy](ctx, atgb.build, atgb, atgb.build.inters, v)
}

func (atgb *AudioTrackGroupBy) sqlScan(ctx context.Context, root *AudioTrackQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(atgb.fns))
	for _, fn := range atgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*atgb.flds)+len(atgb.fns))
		for _, f := range *atgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*atgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := atgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AudioTrackSelect is the builder for selecting fields of AudioTrack entities.
type AudioTrackSelect struct {
	*AudioTrackQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ats *AudioTrackSelect) Aggregate(fns ...AggregateFunc) *AudioTrackSelect {
	ats.fns = append(ats.fns, fns...)
	return ats
}

// Scan applies the selector query and scans the result into the given value.
func (ats *AudioTrackSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ats.ctx, "Select")
	if err := ats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AudioTrackQuery, *AudioTrackSelect](ctx, ats.AudioTrackQuery, ats, ats.inters, v)
}

func (ats *AudioTrackSelect) sqlScan(ctx context.Context, root *AudioTrackQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ats.fns))
	for _, fn := range ats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
