// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/altierawr/vstreamer/ent/playbackclient"
	"github.com/altierawr/vstreamer/ent/playsession"
)

// PlaybackClientCreate is the builder for creating a PlaybackClient entity.
type PlaybackClientCreate struct {
	config
	mutation *PlaybackClientMutation
	hooks    []Hook
}

// SetIsBuffered sets the "is_buffered" field.
func (pcc *PlaybackClientCreate) SetIsBuffered(b bool) *PlaybackClientCreate {
	pcc.mutation.SetIsBuffered(b)
	return pcc
}

// SetNillableIsBuffered sets the "is_buffered" field if the given value is not nil.
func (pcc *PlaybackClientCreate) SetNillableIsBuffered(b *bool) *PlaybackClientCreate {
	if b != nil {
		pcc.SetIsBuffered(*b)
	}
	return pcc
}

// SetSessionID sets the "session" edge to the PlaySession entity by ID.
func (pcc *PlaybackClientCreate) SetSessionID(id int) *PlaybackClientCreate {
	pcc.mutation.SetSessionID(id)
	return pcc
}

// SetNillableSessionID sets the "session" edge to the PlaySession entity by ID if the given value is not nil.
func (pcc *PlaybackClientCreate) SetNillableSessionID(id *int) *PlaybackClientCreate {
	if id != nil {
		pcc = pcc.SetSessionID(*id)
	}
	return pcc
}

// SetSession sets the "session" edge to the PlaySession entity.
func (pcc *PlaybackClientCreate) SetSession(p *PlaySession) *PlaybackClientCreate {
	return pcc.SetSessionID(p.ID)
}

// Mutation returns the PlaybackClientMutation object of the builder.
func (pcc *PlaybackClientCreate) Mutation() *PlaybackClientMutation {
	return pcc.mutation
}

// Save creates the PlaybackClient in the database.
func (pcc *PlaybackClientCreate) Save(ctx context.Context) (*PlaybackClient, error) {
	pcc.defaults()
	return withHooks(ctx, pcc.sqlSave, pcc.mutation, pcc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pcc *PlaybackClientCreate) SaveX(ctx context.Context) *PlaybackClient {
	v, err := pcc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcc *PlaybackClientCreate) Exec(ctx context.Context) error {
	_, err := pcc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcc *PlaybackClientCreate) ExecX(ctx context.Context) {
	if err := pcc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pcc *PlaybackClientCreate) defaults() {
	if _, ok := pcc.mutation.IsBuffered(); !ok {
		v := playbackclient.DefaultIsBuffered
		pcc.mutation.SetIsBuffered(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pcc *PlaybackClientCreate) check() error {
	if _, ok := pcc.mutation.IsBuffered(); !ok {
		return &ValidationError{Name: "is_buffered", err: errors.New(`ent: missing required field "PlaybackClient.is_buffered"`)}
	}
	return nil
}

func (pcc *PlaybackClientCreate) sqlSave(ctx context.Context) (*PlaybackClient, error) {
	if err := pcc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pcc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pcc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pcc.mutation.id = &_node.ID
	pcc.mutation.done = true
	return _node, nil
}

func (pcc *PlaybackClientCreate) createSpec() (*PlaybackClient, *sqlgraph.CreateSpec) {
	var (
		_node = &PlaybackClient{config: pcc.config}
		_spec = sqlgraph.NewCreateSpec(playbackclient.Table, sqlgraph.NewFieldSpec(playbackclient.FieldID, field.TypeInt))
	)
	if value, ok := pcc.mutation.IsBuffered(); ok {
		_spec.SetField(playbackclient.FieldIsBuffered, field.TypeBool, value)
		_node.IsBuffered = value
	}
	if nodes := pcc.mutation.SessionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   playbackclient.SessionTable,
			Columns: []string{playbackclient.SessionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(playsession.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.play_session_clients = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PlaybackClientCreateBulk is the builder for creating many PlaybackClient entities in bulk.
type PlaybackClientCreateBulk struct {
	config
	err      error
	builders []*PlaybackClientCreate
}

// Save creates the PlaybackClient entities in the database.
func (pccb *PlaybackClientCreateBulk) Save(ctx context.Context) ([]*PlaybackClient, error) {
	if pccb.err != nil {
		return nil, pccb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(pccb.builders))
	nodes := make([]*PlaybackClient, len(pccb.builders))
	mutators := make([]Mutator, len(pccb.builders))
	for i := range pccb.builders {
		func(i int, root context.Context) {
			builder := pccb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PlaybackClientMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pccb *PlaybackClientCreateBulk) SaveX(ctx context.Context) []*PlaybackClient {
	v, err := pccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pccb *PlaybackClientCreateBulk) Exec(ctx context.Context) error {
	_, err := pccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pccb *PlaybackClientCreateBulk) ExecX(ctx context.Context) {
	if err := pccb.Exec(ctx); err != nil {
		panic(err)
	}
}
