input PlayVideoInput {
  videoID: ID!
}

type Mutation {
  createVideo(input: CreateVideoInput!): Video!
  createLibrary(input: CreateLibraryInput!): Library!
  playVideo(input: PlayVideoInput!): PlaySession!
}
